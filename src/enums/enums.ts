export enum TOKEN_TYPE {
  accessToken = 'accessToken',
  refreshToken = 'refreshToken'
}

export enum ORDER_STATUS {
  paid = 'PAID',
  pending = 'PENDING',
  canceled = 'CANCELED',
  delivered = 'DELIVERED'
}
